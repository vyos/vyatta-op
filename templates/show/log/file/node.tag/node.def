help: Show contents of user-defined log file
allowed: local -a array ;
         array=( /var/log/user/* ) ;
         echo  -n ${array[@]##*/}
run: if [ -f /var/log/user/$4 ]; then
	cat /var/log/user/$4;
     else
	echo "no such file";
     fi
