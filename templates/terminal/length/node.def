help: "Set terminal page length [0 will disable paging]"
run: if [ -n "$3" ] ; then
	if [ $3 -eq 0 ] ; then
	   OFR_PAGER='cat -s' ;
	else
	   export LINES=$3 ;
	   stty rows $3 ;
	fi ;
     else
        eval $( resize ) ;
     fi ;
     : ${OFR_PAGER:=${OFR_DEFAULT_PAGER}}
